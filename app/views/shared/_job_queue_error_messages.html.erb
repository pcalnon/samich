<% if @job_queue.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@job_queue.errors.count, "error") %>.
    </div>
    <ul>
    <% @job_queue.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
